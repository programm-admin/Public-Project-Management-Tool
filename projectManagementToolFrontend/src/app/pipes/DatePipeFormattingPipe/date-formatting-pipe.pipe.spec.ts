import { DateFormattingPipePipe } from './date-formatting-pipe.pipe';

describe('DateFormattingPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormattingPipePipe();
    expect(pipe).toBeTruthy();
  });
});
