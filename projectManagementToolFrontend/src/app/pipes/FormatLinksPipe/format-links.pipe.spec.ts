import { FormatLinksPipe } from './format-links.pipe';

describe('FormatLinksPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatLinksPipe();
    expect(pipe).toBeTruthy();
  });
});
