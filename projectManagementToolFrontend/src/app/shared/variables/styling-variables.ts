import { TooltipPosition } from '../types/styling-types';

const AFTER: TooltipPosition = 'after';
const BEFORE: TooltipPosition = 'before';
const ABOVE: TooltipPosition = 'above';
const BELOW: TooltipPosition = 'below';
const LEFT: TooltipPosition = 'left';
const RIGHT: TooltipPosition = 'right';

export const TOOLTIP_POSITION = {
  AFTER,
  BEFORE,
  ABOVE,
  BELOW,
  LEFT,
  RIGHT,
};
